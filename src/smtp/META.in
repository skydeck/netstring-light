requires = "netstring equeue"
version = "@VERSION@"
description = "Ocamlnet - Simple Mail Transfer Protocol (SMTP)"
archive(byte) = "smtp.cma"
archive(native) = "smtp.cmxa"
archive(native,gprof) = "smtp.p.cmxa"
